<%= form_for(flight) do |f| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :origin %>
    <%= f.text_area :origin %>
  </div>

  <div class="field">
    <%= f.label :destination %>
    <%= f.text_area :destination %>
  </div>

  <div class="field">
    <%= f.label :plane_id %>
    <%= f.number_field :plane_id %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="field">
    <%= f.label :remaining_seats %>
    <%= f.number_field :remaining_seats %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
